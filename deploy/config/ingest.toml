# Production-shaped canonical writer configuration for the
# single-host Compose topology. Environment variables may override any field.

[network]
name = "zcash-mainnet"

[node]
json_rpc_addr = "http://zebra:8232"
indexer_grpc_addr = "http://zebra:8230"
request_timeout_secs = 30
max_response_bytes = 67108864

[node.auth]
method = "cookie"
path = "/var/run/auth/.cookie"

[storage]
path = "/var/lib/zinder/canonical"
raw_blob_policy = "transactions"

[ingest]
source = "zebra-json-rpc"
projection_preset = "wallet"
reorg_window_blocks = 100

[ingest.run_overrides]
coverage = "wallet-serving"

[ingest_control]
listen_addr = "127.0.0.1:9100"
checkpoint_staging_root = "/var/lib/zinder/checkpoints"
bearer_token_path = "/var/run/zinder-control/ingest.token"
checkpoint_bearer_token_path = "/var/run/zinder-checkpoint/checkpoint.token"

[ops]
listen_addr = "127.0.0.1:9105"
